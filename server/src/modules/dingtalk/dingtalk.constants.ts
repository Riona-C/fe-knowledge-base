/** 钉钉消息转文档 AI 提示词 */
export const DINGTALK_AI_PROMPT = `你是一个前端技术文档整理专家，请把下面的聊天消息整理成标准的前端问题文档：
1. 问题标题（简洁精准）
2. 问题描述（清晰描述问题现象）
3. 解决方案（详细、可直接使用）
4. 技术标签（最多3个）
输出格式严格使用JSON，不要其他内容：
{"title":"","problem":"","solution":"","tags":""}`;

/** 群聊自动检测关键词列表（统一小写，匹配时不区分大小写） */
export const AUTO_DETECT_KEYWORDS = [
  '问题', '报错', '异常', '错误', '出错',
  '解决方案', '解决了', '解决办法', '怎么解决', '搞定了',
  '兼容问题', '兼容性', '不兼容',
  '低端机', '低端设备', '低端手机',
  '花屏', '闪屏', '白屏', '黑屏', '卡顿', '崩溃', '闪退',
  '内存泄漏', '内存溢出',
  '踩坑', '踩了个坑', '坑点', '避坑',
  '样式错乱', '布局错乱', '页面错乱',
  '回退', '降级',
  '适配', '屏幕适配', '机型适配',
  '构建失败', '编译失败', '打包失败',
  '404', '500', '403', '跨域', 'cors',
  '热更新失败', 'hmr',
  '依赖冲突', '版本冲突',
  '样式穿透', '深度选择器',
  '性能优化', '首屏', '懒加载',
];

/**
 * 需要词边界匹配的英文关键词（避免 debug 误触发 bug 等场景）
 * 匹配规则：前后必须为非字母字符或字符串边界
 */
export const AUTO_DETECT_WORD_KEYWORDS = [
  'bug', 'crash', 'oom', 'polyfill',
];

/** 自动检测 AI 提示词（含上下文聚合 + 智能分类） */
export const AUTO_DETECT_AI_PROMPT = `你是一个资深前端工程师，擅长从团队群聊中识别和归纳技术问题。

## 任务
分析下方群聊消息，判断是否包含有价值的前端技术问题/踩坑经验，如果有，提取并整理为结构化文档。

## 分析要点
1. 关注消息中的**具体技术现象**（报错信息、异常表现、兼容性问题等）
2. 关注**解决过程**（谁提出了方案、最终怎么解决的）
3. 多人对话中，综合所有人的发言还原完整问题和方案
4. 如果有人贴了代码片段或配置，保留关键部分

## 输出字段
- title: 问题标题，简洁精准（不超过50字），格式如"xxx场景下xxx问题"
- problem: 问题描述（Markdown），包含：现象、复现条件、影响范围、关键报错信息
- solution: 解决方案（Markdown），包含：具体步骤、关键代码/配置、注意事项。如果聊天中没有给出方案，填"待补充"
- tags: 最多3个技术标签，逗号分隔（如：Vue3,iOS兼容,白屏）
- categoryName: 必须从以下分类中选择最匹配的一个（原样输出分类名）：{{categories}}

## 判断规则
- 有明确技术问题 → 正常输出
- 只有吐槽/讨论但无具体问题 → {"skip": true}
- 纯闲聊/非技术内容 → {"skip": true}

严格输出 JSON，不要其他内容：
{"title":"","problem":"","solution":"","tags":"","categoryName":""}`;

/** 上下文窗口大小（取最近多少条消息） */
export const CONTEXT_WINDOW_SIZE = 20;

/** 上下文时间窗口（只拉取最近多少分钟内的消息，避免无关旧消息干扰） */
export const CONTEXT_TIME_WINDOW_MIN = 30;

/** 自动检测防抖间隔（同一会话连续命中关键词时，多久内不重复触发，单位毫秒） */
export const AUTO_DETECT_DEBOUNCE_MS = 5 * 60 * 1000;

/** @机器人 → 成功收录回复 */
export const AT_BOT_REPLY_SUCCESS = `### 📝 已收录到前端知识库

**标题**: {{title}}
**分类**: {{categoryName}}
**标签**: {{tags}}

> 问题已自动提取并提交审核，审核通过后可在知识库中搜索查看。`;

/** @机器人 → 未检测到有效内容回复 */
export const AT_BOT_REPLY_SKIP = `### 💡 未检测到有效技术问题

消息内容中未识别到具体的前端技术问题或解决方案，暂不收录。

**提示**: 确保@我时附带具体的问题描述或解决方案~`;

/** @机器人 → 消息内容过短/空泛时的使用引导 */
export const AT_BOT_REPLY_USAGE = `### 📖 使用方式

请直接 @我 + 技术问题描述，例如：

> @知识库 iOS Safari 下 fixed 定位元素在键盘弹起时被顶上去，用 position:sticky 替代后解决

支持的内容格式：
- 问题描述 + 解决方案
- 报错信息 + 排查过程
- 踩坑经验总结

我会自动提取并归档到知识库 📝`;

/** 判定为空泛内容的关键词/模式（用户@bot但没有实际技术内容时匹配） */
export const AT_BOT_EMPTY_PATTERNS = [
  '收集', '帮忙收集', '收集以上', '收集问题',
  '记录', '帮忙记录', '归档', '整理',
  '你好', '在吗', '帮忙', '谢谢',
];
